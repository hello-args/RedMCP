name: MCPAudit Security Scan
description: Scan an MCP server for security vulnerabilities
branding:
  icon: shield
  color: red

inputs:
  target:
    description: Path to MCP server entrypoint
    required: true
    default: ./server.py
  fail-on-critical:
    description: Fail the workflow if critical findings exist
    required: false
    default: "true"

runs:
  using: composite
  steps:
    - name: Install MCPAudit
      shell: bash
      run: pip install mcpaudit

    - name: Run security scan
      shell: bash
      run: |
        ARGS="${{ inputs.target }}"
        if [ "${{ inputs.fail-on-critical }}" = "true" ]; then
          mcpaudit scan "$ARGS" --fail-on-critical -o mcpaudit-report.json
        else
          mcpaudit scan "$ARGS" -o mcpaudit-report.json
        fi

    - name: Upload report
      uses: actions/upload-artifact@v4
      with:
        name: mcpaudit-report
        path: mcpaudit-report.json
