# MCTS brand assets

| Asset | Use |
|-------|-----|
| ASCII wordmark (`mcts/ui/logo.py`) | Terminal scan header |
| `report/assets/icons/shield.svg` | HTML dashboard sidebar icon (embedded as data URI) |

HTML reports load the shield icon via `mcts.brand.logo_data_uri()`.
